# Optional/dev dependencies (install only when needed)
transformers>=4.43,<5.0
# For vLLM-based loading/inference (optional)
vllm>=0.4.0,<1.0
# PyTorch (install a CPU-only build locally if no GPU)
torch>=2.3,<3.0
# Test tooling
pytest==8.2.0
pytest-cov==4.1.0
